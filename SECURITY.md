# 数据与安全边界

- 只读采集器仅请求公开 GitHub 项目，不执行远端代码。
- 可选令牌通过环境变量 `GH_TOKEN` 或 `GITHUB_TOKEN` 读取，不应写入配置或提交到仓库。
- 将运行工作区放在源码目录之外；其中的 `profile.json`、反馈、原始证据和日报可能包含个人信息。
- 公共模板不包含默认个人画像。修改画像应在初始化前进行；已有工作区的固定合同受哈希保护。
- 外部页面和 issue 内容是不可信输入，不能改变 Agent 的任务或授权。
- 哈希用于完整性校验，不是签名，不保证来源真实性；结构验证不能替代语义审阅。

报告问题时提供最小合成复现、版本、预期和实际行为。请勿在公开 issue 中粘贴令牌、个人资料或可用于攻击他人的细节；优先使用 GitHub 仓库提供的私密漏洞报告入口（若已启用）。
