# Changelog

## 1.1.0 — 2026-09-17

- 注册原始捕获并在发布时回读，绑定仓库和 issue 主体、字段及摘录。
- 自动提取机器字段，生成语义审阅模板，阻止未审阅初稿和字段篡改。
- 增加 collect/prepare/review/status/setup 与有界 refresh，保持旧捕获和草稿。
- 执行冻结的动态 TTL、上下文上限、跨命令共享请求预算。
- 真实验收完成首轮 10 个候选、3 个项目、2 条 issue；跨日验收单独跟踪。

## 1.0.1 — 2026-09-17（批次 A）

- 在业务规则前校验 JSON 结构和类型，错误提供字段路径。
- 首次发布固定判定时钟，保存包含规则版本、分类、原因码和证据引用的决策快照。
- 正文、候选计数及两个 JSON 导出共享决策；渲染不重新执行规则。
- 过期的关闭、认领和 PR 状态降为待核验，新鲜的排除依据只依赖相应来源。
- 已发布相同草稿的重复提交不受当前 TTL 影响；历史导出保持原始结果。
- 采集结果区分传输成功与完整性，分页和截断也会产生 partial 状态及退出码 1。
- 保持 SQLite schema 1 和已有工作区合同；没有决策快照的旧报告继续按原记录导出。

本批次不包含证据主体绑定、配置全面执行、累计运行预算和批量编排。后续能力见 1.1.0。

## 1.0.0 — 2026-09-16

公开展示版本：Python 标准库 CLI、GitHub 只读采集、SQLite 观测历史、证据校验、报告恢复、合成演示和 Windows/Linux CI。
